 <h1>
 	<%= link_to "Все продукты", products_path %>
 	<% if params[:category] %>
 	→ <%= link_to @current_category.name, category_path %>
 	<% end %>
 	<% if params[:subcategory] %>
	→ <%= link_to @current_subcategory.name, subcategory_path %>
 	<% end %>
 </h1>

<%= will_paginate @products, next_label: "След →", previous_label: "← Пред" %>
    <div id="store">
     <%= render :partial => "products/product", collection: @products %>
    </div>
<%= will_paginate @products, next_label: "След →", previous_label: "← Пред" %>