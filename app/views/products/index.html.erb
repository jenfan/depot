<% if !@products.empty? %>
	<%= will_paginate @products, next_label: "След →", previous_label: "← Пред" %>
	    <div id="store">
	     <%= render @products %>
	    </div>
	<%= will_paginate @products, next_label: "След →", previous_label: "← Пред" %>
<% else %>
<p>По данному запросу товаров не найдено.</p>
<% end %>