<% if line_item == @current_item %>
	<tr id = "current_item" >
<% else %>
	<tr>
<% end %>
 <td><%= link_to 'del', remove_item_line_item_path(id: line_item), remote: true %><%= line_item.quantity %>&times;<%= number_to_currency(line_item.product.price) %></td>
 <td><%= line_item.product.title %></td>
 <td class="item_price"><%= number_to_currency(line_item.total_price) %></td>
</tr>